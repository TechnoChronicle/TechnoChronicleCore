pluginManagement {
    repositories {
        flatDir {
            dir 'libs'
        }
        mavenLocal()
        gradlePluginPortal()
    }
}

plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.9.0'
}
